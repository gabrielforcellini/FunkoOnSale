import React from 'react';
import * as Styled from './styles';

import { FormLogin } from '../../components/FormLogin';


export const Login = () => { 
    return (
        <Styled.Container>
            <FormLogin />
        </Styled.Container>
    );
};
